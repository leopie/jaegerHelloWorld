server:
  port: 8081
spring:
  application:
    name: Big-Brother
  datasource:
    password: password93
    url: jdbc:postgresql://localhost:5432/jaeger_chapter4
    username: leopie
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true